pytest-homeassistant-custom-component==0.13.*
ruff>=0.6

# Mirrors the integration's own requirements in manifest.json. Home Assistant
# installs those at runtime; CI has to be told.
python-socketio[asyncio_client]>=5.11,<6
