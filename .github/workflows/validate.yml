name: Validate

on:
  push:
    branches: [main]
  pull_request:
  # Weekly, because hassfest and the HACS action track Home Assistant and can
  # start failing without anything here changing.
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  hassfest:
    name: hassfest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: home-assistant/actions/hassfest@master

  hacs:
    name: HACS
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: hacs/action@main
        with:
          category: integration
          # No brand assets yet. Drop this once custom_components/kumo_cloud/brand
          # carries icon.png and logo.png.
          ignore: brands
